#!/usr/bin/env ruby

require File.expand_path('../../config/environment', __FILE__)

serial = SerialPort.new('/dev/rfcomm0')
bt = BlueTherm::SerialConnection.new(serial)

test = Object.new

def test.update(m)
  puts m
end

bt.add_observer(test)

p = BlueTherm::Packet.default

pry

